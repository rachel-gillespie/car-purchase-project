Name : Rachel Gillespie

Student Number: 20118715

Coding Process and Challenges
-----------------------------
For this project, I started by creating 2 classes in the Sources Root folder, CarPurchaseHelper and Driver. 

The CarPurchaseHelper class contains all the car purchase calculation methods, HP, PCP, Fuel Cost, Monthly Cost and Recommended Car Type. It also contains some extra methods, a Return to Menu method, a method to print the Welcome Messsage, and the Goodbye Message.

The Driver class contains the main method and the Scanner class.
______________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
Secondly, after creating the 2 classes I began writing the methods in the CarPurchaseHelper.java file, below I detail what the methods do:

printWelcomeMessage (line 5 - 7)
- When called the method returns the message "Welcome to the Car Purchase Helper Program! Let's find the perfect car for you!". 

calculateHP (line 10 - 31)
A finance plan where you pay a deposit and then equal monthly payments until the full amount is paid. You own the car once the final payment is made.
- This method is used to calculate the monthly Hire Purchase payments. 
- It takes in carPrice, deposit, annualInterest and termYears from the user to calculate HP.
- amountToFinance is calculated by subtracting deposit from carPrice.
- monthlyInterestRate is calculated by dividing annualInterest by 100 and the answer by 12.
- totalMonths is calculated by multiplying termYears by 12.
- It then uses the formula (1 + r)^n.
- r is the monthlyInterestRate and n is the number of totalMonths.
- A for loop is used to calculate (1 + r)^n, it is then stored as a variable called factor.
- Factor, amountToFinance and monthlyInterestRate are then used to calculate the monthly payment using the formula, monthlyPayment = L * r * factor / (factor - 1).
- L is the amountToFinance and r is the monthlyInterestRate.

pcpResultMonth (line 34 - 56)
A type of car finance where you pay a deposit, followed by monthly payments over a fixed term. At the end, you can either return the car, pay a final balloon payment to own it, or trade it in.
- This method is used to calculate the monthly Personal Contract Plan payments.
- It takes the carPrice, deposit, annualInterest, termYears and gmfvPercent from the user to calculate PCP.
- gmfv is calculated by dividing gmfvPercent by 100 and then mulitplying carPrice by that answer.
- amountToFinance is calculated by subtracting deposit from carPrice and then subtracting gmfv.
- monthlyInterestRate is calculated by dividing annualInterest by 100 and the answer by 12.
- totalMonths is calculated by multiplying termYears by 12.
- It then uses the formula (1 + r)^n.
- r is the monthlyInterestRate and n is the number of totalMonths.
- A for loop is used to calculate (1 + r)^n, it is then stored as a variable called factor.
- Factor, amountToFinance and monthlyInterestRate are then used to calculate the monthly payment using the formula, monthlyPayment = L * r * factor / (factor - 1).
- L is the amountToFinance and r is the monthlyInterestRate.

fuelCostEstimator (line 58 - 65)
- This method is used to calculate weekly Fuel Cost.
- It takes the weeklyKilometers and fuelCost per litre from the user input to calculate weeklyFuelCost.
- weeklyFuelCost is calculated by dividing weekly kilometers by 100 multiplying the answer by 6 and then by fuelCost.
- Assuming the car uses 6L of fuel per 100km.

calculateMonthlyCost (line 67 - 74)
- This method is used to calculate monthly cost.
- It takes the carPrice and termMonths from the user input to calculate monthlyPayment.
- monthlyPayment is calculated by dividing carPrice by termMonths.

recommendCarType (line 76 - 87)
- This method is used to recommend a car type to the user.
- It takes the carRequiremnt from the user input.
- Based on the requirement entered it returns a recommended car.
- If the user doesn't enter a recognised requirement this message is returned "You did not input a valid requirement!".

returnToMenu (line 89 - 91) ***
- This method is used to bring the user back to the main menu.
- When called it checks to see if the user input is equal to "yes" while ignoring letter case, it returns true or false depending if it is equal or not.

printGoodbyeMessage (line 93 - 95)
- When called the method returns the message "We hope you had a successful experience! Goodbye!"

These methods can be called whenever needed in the Driver class, reducing repetition and making the code easier to read and maintain.
______________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
Next, I started working on the Driver.java file.

- The first thing I did in this file was import the Scanner class. (line 3)
- Then I invoked the main method. public: Java Virtual Machine must access it, static: Belongs to class, not an object, void: doesn't return a value and String[] args: Can hold command-line input. (line 6)
- I created an instance of the driver method and then called it. (line 8 & 11)
- I then created a Scanner object called input in order to use the Scanner class. This object is the tool for reading input. (line 13)
- I also created a CarPurchaseHelper object called CarPurchaserHelper to access the methods I created in that class. (line 15)
- The next thing I did was call the printWelcomeMessage() method. (line 17)
- I then assigned the variable choice of type int the value of 0 to initialise the variable. (line 19)
- I created a while loop to repeatedly take input from the user until they choose to exit the program, which then executes the goodbye message when the loop has finished. (line 20 - 177)
- Inside the while loop I then printed out the Main menu and the options the user can choose from. (line 22 - 39)
- After that the variable 'choice' is updated by the user input read by the Scanner class.
- I decided to add a divder between each section to optimise readability and separate the sections. (line 42, 67, 94, 115, 136, 166, 174 & 182) ***
- The next thing was to create an 'if' statement so that the program would print a different calculation depending on the choice the user input. An 'if' statement evaluates a boolean condition and its result will determine which portion of the if statement is executed. The LCV (Loop Control Variable) is updated after every loop. (line 44, 69, 96, 117, 138, 168, 170)
- For the HP Calculation, The user is asked to answer some questions about car price, deposit, annual interest rate and term years. These are stored as variables. The carPurchaserHelper object is used to call the method calculateHP. The monthly HP payment is then printed out as the hpMonthly variable. (line 44 - 65)
- I used the format() method which converts the double hpMonthly, double pcpMonthly, double fuelWeekly and double costMonthly into Strings and formats it to 2 decimal places. (line 58, 85, 106 & 127) ***
- Calling the returnToMenu method, asks the user if they want to return to the menu and waits for user input. Reads what the user types and stores it as the variable 'reply'. The answer is checked using the returnToMenu() method and it makes a decision. If the user doesn't input yes, the choice is set to 6 to exit the loop, the Goodbye message is printed and the program ends. (line 60 - 65, line 87 - 92, line 108 - 113, line 129 - 134 & line 159 - 164) ***
- For the PCP Calculation, The user is asked to answer some questions about car price, deposit, annual interest rate, term years and guaranteed minimum future value. These are stored as variables. The carPurchaserHelper object is used to call the method pcpResultMonth. The monthly PCP payment is then printed out as the pcpResultMonth variable. (line 69 - 96)
- For the Fuel Cost Calculation, The user is asked to answer some questions about weekly kilometers and fuel cost per litre. These are stored as variables. The carPurchaserHelper object is used to call the method fuelCostEstimator. The weekly fuel cost is then printed out as the fuelWeekly variable. (line 96 - 117)
- For the Monthly Cost Calculation, The user is asked to answer some questions about car price and term in months. These are stored as variables. The carPurchaserHelper object is used to call the method calculateMonthlyCost. The monthly cost is then printed out as the costMonthly variable. (line 117 - 138)
- For the Car Type Recommendation, The user is asked to answer a question about car requirement. This is stored as a variable. The carPurchaserHelper object is used to call the method recommendCarType. The recommended car is then printed out as the recommendCar variable. The while loop repeats looking for input from the user until the user types one of the three options suggested. (line 138 - 168)
- An error message will be printed if the user doesn't input a valid option. (line 170 - 175)
- The final thing I did was call the printGoodbyeMessage() method outside the while loop. (line 180)

______________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
Main difficulties I came across in the development of solution and how I solved them:
- Figuring out the order to have everything in so that it repeats in the while loop.
- Dealing with syntax errors.
- Making sure the return to menu worked after each set of questions.
- It's running perfectly now but I've had some difficulty with the buffer.

______________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________
Main learnings from my engagement with assignment:
- The reflection really helped me understand my code fully, by taking the time to write out and explain my process I was able to reflect and double check why I did things the way I did.

Marking Scheme / Rubric
-----------------------------
Part A - Comments/Indention etc (8.5 / 9)
-----------------------------
comments (1 / 1)
formatting (2 / 2)
naming (2 / 2)
structure of code (1.5 / 2)
inclusion of readme and reflection (2 / 2)

Part B - Working Project  (6 / 6)
-----------------------------
Code compile and runs without error (6 / 6)

Part C - CarPurchaseHelper (30 /34)
----------------------------- 
PCP method explained  (5 / 7)   
HP method explained (5 / 7) 
All other methods written   (20 / 20)   

Part D - Driver	 (36 /36)
-----------------------------
CarPurchaseHelper object created (3 /3)
Code written as per spec (33 /33)
   - HP Calculation code uncommented and working (3 /3)
   - PCP code correctly wrtten and outputs amount (15 /15)
   - lcv correctly updating and menu looping (5 /5)
   - all methods created in CarPurchaseHelper called and used correctly (10 /10)

Part E - Reflection/Readme (8 / 10)
-----------------------------
Filled out parts A - D above (3 / 3)

Completed readme as per spec (2 / 2)

Rest (3 / 5)
Chronology of my implementation (what I did first, etc.. )

Main difficulties I came across in the development of solution and how I solved them.

Any bugs remaining in the solution or unfinished elements of spec (no need if you have detailed these in the rubric section).

Main learnings from my engagement with assignment.

Mandatory : PLease list any references used in your development/ implementation of your submission. 
https://www.geeksforgeeks.org/java/loops-in-java/
https://www.geeksforgeeks.org/java/methods-in-java/
https://www.w3schools.com/java/java_booleans.asp
https://www.asciiart.eu/text-to-ascii-art
https://www.w3schools.com/java/ref_string_format.asp

Please consider the following statements and choose one (delete the inappropriate one)

- This is my work apart from the specific references noted above (and any code from class notes). I understand the code and can decribe any parts of the solution if needs be.

Part F - Final Product (4 / 5)
-----------------------------
Well written menu and ease of use (4 / 5)

Part G - For Extra Credit (10%)  (3 / 10)
-----------------------------
Extra Functionality ***
Please list the following for each extra functionality that you implemented:
- I decided to add a divder between each section to optimise readability and separate the sections. (line 42, 67, 94, 115, 136, 166, 174 & 182)
- I used the format() method which converts the double hpMonthly, double pcpMonthly, double fuelWeekly and double costMonthly into Strings and formats it to 2 decimal places. (line 58, 85, 106 & 127)
- returnToMenu method, asks the user if they want to return to the menu and waits for user input. Reads what the user types and stores it as the variable 'reply'. The answer is checked using the returnToMenu() method and it makes a decision. If the user doesn't input yes, the choice is set to 6 to exit the loop, the Goodbye message is printed and the program ends. (line 60 - 65, line 87 - 92, line 108 - 113, line 129 - 134 & line 159 - 164)

Final Mark  (92.5 / 100)
-----------------------------
